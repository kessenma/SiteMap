services:
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      PORT: 3000
      NODE_ENV: production
